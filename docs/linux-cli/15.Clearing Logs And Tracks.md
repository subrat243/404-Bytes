>**Linux log files:
```Copy
cd /var/log
```

>**Shred is a utility used to delete files permanently:
```Copy
shred -vfuz <filename>
```

    -v = show progress (verbose).
    -f = change permissions to allow writing if necessary.
    -u = deallocate and remove file after overwriting.
    -z = add a final overwrite with zeros to hide shredding.

>==**#Note:- It's very important to change or modify the kernal log (kern.log).

##### Clearing Bash History

>**The file `.bash_history` is on home directory for each user. We can change and modify the file using following:-
```Copy
nano .bash_history
```

>**Redirecting null and wiping out the history from `.bash_history`.
```Copy
>.bash_history
```

---
**Next : [[16.Tmux Essentials]]**
